<!DOCTYPE html>
<html>
  <head>
    <title>OnlineRetailShop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
    <!-- Navigation Links within the format controller -->
  <body class="bg-gray-50 min-h-screen">
    <nav class="bg-white shadow-md border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center space-x-4">
            <%= link_to 'Home', root_path, class: 'text-gray-700 hover:text-blue-700 font-medium px-3 py-2 rounded-md text-sm transition-colors' %>
            <%= link_to 'Shopping', shopping_path, class: 'text-gray-700 hover:text-blue-700 font-medium px-3 py-2 rounded-md text-sm transition-colors' %>
          </div>
          <div class="flex items-center space-x-4">
            <% unless logged_in? %>
              <%= link_to 'Login', login_path, class: 'text-gray-700 hover:text-blue-700 font-medium px-3 py-2 rounded-md text-sm transition-colors' %>
            <% else %>
              <%= link_to 'Logout', logout_path, method: :delete, class: 'bg-red-500 hover:bg-red-600 text-white font-medium px-4 py-2 rounded-md text-sm transition-colors' %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% flash.each do |key, message| %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="flash-<%= key %> rounded-md p-4 <%= key == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : key == 'alert' ? 'bg-red-50 text-red-800 border border-red-200' : 'bg-blue-50 text-blue-800 border border-blue-200' %>">
          <%= message %>
        </div>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>
